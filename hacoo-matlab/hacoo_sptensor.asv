% HACOO class for sparse tensor storage.
%
%HACOO methods:

function t = hacoo_sptensor(varargin);
%HACOO Create a sparse tensor using HaCOO storage.

% EMPTY Constructor
if (nargin == 0) || ((nargin == 1) && isempty(varargin{1}))
    t.subs = [];
    t.vals = [];
    t.size = [];
    t = class(t,'sptensor');
    return;
end
